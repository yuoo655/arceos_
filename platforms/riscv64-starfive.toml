# Architecture identifier.
arch = "riscv64"
# Platform identifier.
platform = "riscv64-starfive"
# Platform family.
family = "riscv64-starfive"

# Base address of the whole physical memory.
phys-memory-base = "0x8000_0000"
# Size of the whole physical memory.
phys-memory-size = "0x200_0000"     # 128M
# Base physical address of the kernel image.
kernel-base-paddr = "0x8020_0000"
# Base virtual address of the kernel image.
kernel-base-vaddr = "0xffff_ffc0_8020_0000"
# Linear mapping offset, for quick conversions between physical and virtual
# addresses.
phys-virt-offset = "0xffff_ffc0_0000_0000"
# MMIO regions with format (`base_paddr`, `size`).
mmio-regions = [
    ["0x1002_0000", "0x10000"],      # ethernet@10020000
    ["0x1191_0000", "0x10000"],      # 11910000.pinctrl gpio
    ["0x1800_1000", "0x1000"],      # tdes
    ["0x1800_2000", "0x1000"],      # rdes

    ["0x1801_0000", "0x10000"],      # txbuffer
    ["0x1802_0000", "0x10000"],      # rxbuffer
    ["0x0201_0000", "0x1000"],       # cache_flush

    ["0x10490000", "0x10000"],      # reset-controller@11840000
    ["0x11840000", "0x10000"],      # reset@11840000
]


# Timer interrupt frequency in Hz.
timer-frequency = "10_000_000"      # 10MHz
